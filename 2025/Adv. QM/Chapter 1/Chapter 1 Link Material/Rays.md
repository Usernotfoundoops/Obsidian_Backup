v∈H⟹λv∈H
same physical state

Because physically, only the **direction** of v matters, not its length. Scaling just changes the length, not the direction. This is why states are really **rays** (equation 1.30) — all scaled versions of v are the same physical state.

$[ψ]={λψ ∣ λ∈C∖{0}}[\psi] = \{\lambda\psi \ | \ \lambda \in \mathbb{C}\setminus\{0\}\}[ψ]={λψ ∣ λ∈C∖{0}}$

All these vectors are physically identical. The set of them together is called a **ray.**

What I Learnt:
	Take a state (2,4)
	We can rotate and scale it it by 
		-1:(-2,-4)
		2:(-4,-8)
		i:(2i,4i)
	What happens is, let's take expectation value of P:
		$$\langle P \rangle = \frac{\langle \psi|P|\psi\rangle}{||\psi||^2}$$
	for i rotation:
$$\langle P \rangle = \frac{\langle -i\psi|P|i\psi\rangle}{|i²|*||\psi||^2} =\frac{-i^2\langle \psi|P|\psi\rangle}{|-1|*||\psi||^2}=\frac{\langle \psi|P|\psi\rangle}{||\psi||^2}$$ We get the same expectation value for the Obseravble.