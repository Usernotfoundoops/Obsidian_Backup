1. The complex linear equation $A\vec{x}=\vec{b}$ can be solved by taking the approximate of the expected value of a matrix M. Such that $x^* Mx$ (M is the Observable)
2. M: Sparse NxN(Zeros<=N) and has conditional number k.
3. Classical equation solves in $\tilde{O}(N^2)$, QM: $\tilde{O}(\sqrt{N}*k)$ k scales the inverse exp curve
4. The set of linear equations with N unknowns ($x_1,x_2,x_3 \in \vec{x}$), for N equations. [[Ref.]]
5. Instead of finding x vector we try to find the Exp val of M matrix which should satisfy the above requirement.
6. 